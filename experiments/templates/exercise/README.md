# Exercise template

Copy this entire directory into `src/exercises/ex-NNNN-short-name/`.

While the record is a paper design:

- complete `exercise.ts` and `DESIGN.md`;
- keep `status: 'planned'`;
- keep the component named `Prototype.example.svelte` so discovery ignores it.

When implementation begins:

- rename it to `Prototype.svelte`;
- change the status to `prototyping`;
- replace the generic interface with one bounded exercise loop.
